---
id: IA-20260901-010
created_at: 2026-09-01T00:00:00Z
author: Claude
role: IA (lider de proyecto)
category: feature
status: executed
module: ALFA OS / Cotizaciones / Sprint G4 - Voz a borrador
summary: Dictas un pedido -> un loop de herramientas sobre Claude resuelve cliente/productos/plantillas y crea un borrador via draftBuilder. Endpoint para Atajo de Siri + panel de prueba en /quotes/voz.
owner: Claude
execution_status: executed (pendiente prueba en prod + Atajo de Siri de Leo)
history:
  - 2026-09-01 | Leo | aprobo pasar a G4
  - 2026-09-01 | Claude | tabla quote_voice_drafts, lib/quotes/voiceDraft.ts (agente), endpoint, panel /quotes/voz
---

## Entregado

- `sql/20260901_quote_voice_drafts.sql` (aplicada a prod): registra cada dictado — transcripcion, herramientas llamadas, costo, resultado. RLS select-only.
- `lib/quotes/voiceDraft.ts`: `runVoiceDraft(supabase, { transcript, actor })`. Loop de herramientas sobre Claude (max 8 turnos):
  - `buscar_cliente`, `buscar_producto`, `buscar_mano_de_obra`, `listar_plantillas` (lectura).
  - `crear_borrador` (terminal) -> `buildDraftQuote` / `buildDraftFromTemplate` de G2/G3.
  - `pedir_aclaracion` (terminal) -> devuelve una pregunta.
  - El system prompt le dice: prefiere avanzar con un supuesto (y anotarlo) antes que frenar; nunca inventes ids; ALFA cotiza a precio de catalogo.
  - Candado de costo mensual `QUOTE_VOICE_MONTHLY_CAP_USD` (default $15). Terminos PostgREST saneados (`safeTerm`).
- `app/api/quotes/draft-from-intent/route.ts`: `POST { transcript }`. Auth: `QUOTE_VOICE_SECRET` / `CRON_SECRET` (Bearer o `?key=`) para Siri, o sesion interna. Devuelve `{ status, quote_id, url, assumptions, warnings }` o `{ status: "aclaracion", question }`.
- `app/(admin)/quotes/voz/page.tsx` + `components/quotes/VoiceDraftPanel.tsx`: panel de prueba (textarea + dictado por voz del navegador donde exista + resultado). Link "Dictar" en `/quotes`.

## Verificado

- tsc + build + lint verde. Esquema de `quote_voice_drafts` aplicado.
- **NO probado end-to-end** (necesita `ANTHROPIC_API_KEY`, solo en prod). Primera prueba: `/quotes/voz` con sesion, o `curl -XPOST .../api/quotes/draft-from-intent?key=SECRET -d '{"transcript":"..."}'`.

## Lo que necesita Leo

1. **Env var en Vercel:** `QUOTE_VOICE_SECRET` (un hex nuevo; si no lo pones usa `CRON_SECRET`). Opcional: `QUOTE_VOICE_MONTHLY_CAP_USD` (default 15), `QUOTE_VOICE_MODEL`.
2. **Atajo de Siri** ("Cotiza ALFA"):
   - Accion **Dictar texto** (idioma: Espanol) -> salida = el texto.
   - Accion **Obtener contenido de URL**:
     - URL: `https://portal.alfait.com.mx/api/quotes/draft-from-intent?key=<QUOTE_VOICE_SECRET>`
     - Metodo: POST · Cuerpo: JSON · `transcript` = (Texto dictado)
   - **Obtener valor del diccionario** para `status`, `url`, `question`, `error`.
   - Si `status` es `completada`: **Mostrar notificacion** "Borrador listo" + abrir `url`.
   - Si `status` es `aclaracion`: **Hablar texto** con `question` (y repites el dictado con el dato).
   - Si no: hablar `error`.
   - Nombrar "Cotiza ALFA" -> "Oye Siri, Cotiza ALFA".
3. Probar y decirme si el agente elige bien los productos (eso calibra G5).

## Pendiente / backlog

- El buscador de productos hace OR de todas las palabras sobre marca/modelo/nombre — puede ser ruidoso. Si el agente elige mal seguido, pasar a AND-de-ORs o busqueda por embeddings.
- La aclaracion no mantiene el hilo de conversacion real (re-envia "`<original>`. Aclaracion: `<respuesta>`"). Suficiente para una persona; un hilo real vendria con el PWA.
- G5 (`quote_draft_revisions`) capturara lo que Leo corrige del borrador -> mejora los supuestos del agente.
