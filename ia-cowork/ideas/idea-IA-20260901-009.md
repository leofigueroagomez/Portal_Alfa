---
id: IA-20260901-009
created_at: 2026-09-01T00:00:00Z
author: Claude
role: IA (lider de proyecto)
category: feature
status: executed
module: ALFA OS / Cotizaciones / Sprint G3 - Plantillas
summary: Plantillas de cotizacion (paquetes estandar). Backend + UI. Elegir plantilla -> cliente + cantidades -> borrador via draftBuilder (G2).
owner: Claude (implementacion), Leo (define las plantillas)
execution_status: executed (pendiente prueba en navegador con sesion + que Leo cree las plantillas)
history:
  - 2026-09-01 | Leo | aprobo arrancar G3
  - 2026-09-01 | Claude | tablas, lib/quotes/templates.ts, server actions, UI QuoteTemplatesManager, link desde /quotes
---

## Entregado

- `sql/20260901_quote_templates.sql` (aplicada a prod): `quote_templates` + `quote_template_lines` (kind product/labor, FK a `products` / `labor_activity_catalog` con `on delete set null` para tolerar borrados). RLS select-only.
- `lib/quotes/templates.ts`: `listQuoteTemplates`, `getQuoteTemplate` (resuelven etiquetas y precio de catalogo, marcan lineas `broken`), `buildDraftFromTemplate({templateId, clientId, quantityOverrides?, notes?})` -> llama a `buildDraftQuote` (G2). Omite lineas rotas y las cuenta.
- `app/(admin)/quotes/templates/actions.ts`: `saveQuoteTemplate` / `deleteQuoteTemplate` / `setQuoteTemplateActive` (gate `admin`/`direccion`), `createDraftFromTemplateAction` (cualquier rol interno).
- `app/(admin)/quotes/templates/page.tsx` + `components/quotes/QuoteTemplatesManager.tsx`: lista de plantillas (tarjetas), flujo "usar" (ClientSearchSelect + ajuste de cantidades + notas -> crea borrador y redirige a `/quotes/{id}/edit`), editor CRUD mobile-first.
- Link "Plantillas" en `app/(admin)/quotes/page.tsx`.

## Verificado

- tsc + build + lint verde.
- Esquema y logica de resolucion probados contra prod (insert + join + cascade delete de una plantilla de prueba).
- ALFA cotiza a precio de catalogo -> las lineas NO guardan margen; `buildDraftQuote` toma `products.calculated_sale_price`.

## Pendiente

- Prueba en navegador con sesion (la pagina es admin-authed; no se pudo previsualizar).
- **Leo:** crear las 5-10 plantillas reales (CCTV residencial 4 cam, control acceso 1 puerta, red casa, etc.).

## Handoff Antigravity (pulido de UI, opcional)

- El buscador de productos del editor es un `<input>` + lista filtrada simple (top 20). Si se vuelve incomodo con 555 productos, un combobox con teclado seria mejor.
- El flujo "usar" no muestra los `warnings` de `buildDraftQuote` (p. ej. "producto sin costo") antes de redirigir — el usuario los ve en el editor. Se podria mostrar un resumen intermedio.
- Considerar una entrada directa a `/quotes/templates` desde el `MobileBottomNav` o desde el FAB "+Cotizacion" (hoy solo hay link en `/quotes`).
