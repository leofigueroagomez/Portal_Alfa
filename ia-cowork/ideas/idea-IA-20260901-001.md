---
id: IA-20260901-001
created_at: 2026-09-01T00:00:00Z
author: Claude
role: IA (lider de proyecto)
category: mejora
status: accepted
module: ALFA OS / El Vigia / Bandeja - Ejecutores B1
summary: Backend de los ejecutores de 1 clic (B1) listo. Falta cablear los botones en la UI (Antigravity).
owner: Claude (backend, hecho) + Antigravity (botones UI)
execution_status: backend hecho; UI pendiente
history:
  - 2026-09-01 | Claude | created - Sprint B1 backend entregado y probado contra prod
---

## Backend entregado (Claude)

- `sql/20260901_vigia_action_backups.sql` (aplicada a prod): tabla `vigia_action_backups` con `snapshot` jsonb, `applied_at`, `reverted_at`.
- `lib/vigia/executors.ts`: registro `EXECUTORS` con 3 ejecutores. Cada uno: `canApply()` (candado de seguridad), `apply()` (guarda snapshot), `revert()`.
  - `recalcular_lineas_de_compra` (INT-09): recalcula costos/totales desde la base operativa. Seguro. **Probado end-to-end contra prod** (aplicar + revertir, 14 lineas, restauracion 100%).
  - `fusionar_linea_huerfana` (INT-02): mueve las compras a la linea sana y borra la huerfana. **Se niega** si: no hay linea sana unica, la linea sana ya tiene compras (doble captura), o hay entregas de material ligadas. Verificado: se niega correctamente el unico candidato actual (hallazgo #4, Sauces 21).
  - `consolidar_partidas_operativas` (INT-01): por ahora solo diagnostica y se niega (el caso con huella es el del proyecto 48, delicado; el caso limpio se validara con un finding real).
- `app/(admin)/vigia/execute-actions.ts`: server actions `applyFindingAction(findingId)` y `revertFindingAction(findingId)`. Gate de rol `admin`/`direccion`. Al aplicar: hallazgo -> `resuelto` + audit `action_applied`. Al revertir: -> `abierto` + audit `action_reverted`.
- `lib/vigia/queries.ts`: `getVigiaFindings` ahora anota cada hallazgo con `executor_label`, `can_apply`, `can_apply_reason`, `has_active_backup`.

## Pendiente - Antigravity (UI en `components/vigia/FindingCard.tsx`)

Para cada hallazgo con `executor_label != null`:
- Si `can_apply === true`: boton **[Autorizar y ejecutar]** -> llama `applyFindingAction(finding.id)`. Al volver `ok`, mostrar el `summary`.
- Si `can_apply === false`: mostrar el `can_apply_reason` en gris (por que NO se puede automatizar), sin boton.
- Si `has_active_backup === true` (hallazgo ya resuelto por un ejecutor): boton **[Revertir]** -> llama `revertFindingAction(finding.id)`.

Nota: las server actions ya estan en un archivo aparte (`execute-actions.ts`), no en tu `actions.ts`, para evitar choques.
