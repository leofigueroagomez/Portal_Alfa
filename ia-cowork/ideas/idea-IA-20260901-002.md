---
id: IA-20260901-002
created_at: 2026-09-01T00:00:00Z
author: Claude
role: IA (lider de proyecto)
category: mejora
status: accepted
module: ALFA OS / El Vigia / Investigar a fondo (Sprint B2)
summary: "Investigar a fondo" un hallazgo bajo demanda. Hibrido: playbook determinista + 1 llamada a Claude. Tope mensual de costo. Correo aparte. Automatico en severity=critico.
owner: Claude
execution_status: executed (backend + UI minima + docs). Falta prueba end-to-end en prod.
history:
  - 2026-09-01 | Claude | created - decisiones de Leo: hibrido, Vercel Pro, tope $25/mes, auto en critico, correo aparte
  - 2026-09-01 | Claude | executed - tablas, lib/vigia/investigate/*, boton en FindingCard, runAutoInvestigations en el cron, docs
---

## Decisiones de Leo (2026-09-01)

- Motor: **hibrido** (playbook SQL determinista + 1 llamada a Claude que interpreta).
- Infra: **Vercel Pro** comprado. `ANTHROPIC_API_KEY` cargada en prod.
- Tope: **$25 USD/mes** duro (`VIGIA_INVESTIGATE_MONTHLY_CAP_USD`). Al alcanzarlo -> `sin_presupuesto`, no se llama al modelo.
- Disparo: manual (boton en la Bandeja) **+ automatico** para `severity = critico` (hasta 3 por corrida del cron).
- Entrega: **correo aparte** (asunto propio, no se mezcla con el brief diario).

## Entregado

| Pieza | Archivo |
| --- | --- |
| Tabla | `sql/20260901_vigia_investigations.sql` (aplicada a prod, migracion `vigia_investigations`) |
| Tipos | `lib/vigia/investigate/types.ts` |
| Candado de costo | `lib/vigia/investigate/budget.ts` (gasto del mes = suma `cost_usd`) |
| Playbooks | `lib/vigia/investigate/playbooks.ts` — `INT-01` (patron proyecto 48) + `generico` |
| Interpretacion | `lib/vigia/investigate/interpret.ts` — 1 llamada, structured output via tool use |
| Correo | `lib/vigia/investigate/email.ts` — Resend, estilo oscuro ALFA |
| Orquestador | `lib/vigia/investigate/index.ts` — `investigateFinding()` + `runAutoInvestigations()` |
| Accion manual | `app/(admin)/vigia/investigate-actions.ts` — gate rol admin/direccion |
| Cron | `app/api/vigia/cron/daily/route.ts` — corre `runAutoInvestigations()` tras el brief |
| UI | `components/vigia/FindingCard.tsx` — bloque "Investigar a fondo" + resultado inline |
| Pagina | `app/(admin)/vigia/page.tsx` — `maxDuration = 300` para la server action |

## Garantias

- Playbooks y expediente: **solo lectura** sobre tablas de negocio.
- El modelo solo interpreta y **propone**; nunca escribe. Su salida es el correo + la fila en `vigia_investigations`.
- El system prompt le prohibe recomendar tocar datos fiscales / CFDI.
- Si falta `ANTHROPIC_API_KEY`, la investigacion falla con mensaje claro; el resto del Vigia no se entera.

## Pendiente

- Probar end-to-end en prod: ahora mismo no hay hallazgo `critico` ni `INT-01` abierto, asi que el playbook INT-01 y el auto-trigger no se han ejercitado con datos reales. Primera oportunidad: cuando aparezca uno, o forzar `POST /api/vigia/investigate`-equivalente (no existe endpoint aun; se dispara desde la Bandeja).
- Playbook dedicado para `INT-04` (mismo patron de duplicado, hoy cae al generico).
- Antigravity: si se hace la pestana "Investigaciones" en la Bandeja, leer de `vigia_investigations` (RLS select-only ya puesta).
